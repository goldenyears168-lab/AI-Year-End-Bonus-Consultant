# 🚀 使用說明

## ✅ 虛擬環境已建立！

您的系統使用 Homebrew 管理的 Python，需要虛擬環境來安裝套件。

## 🎯 啟動應用（三種方法）

### 方法一：使用啟動腳本（最簡單）⭐

```bash
./啟動.sh
```

### 方法二：手動啟動

```bash
# 1. 啟動虛擬環境
source venv/bin/activate

# 2. 啟動應用
streamlit run main.py
```

### 方法三：一行命令

```bash
source venv/bin/activate && streamlit run main.py
```

## 📝 重要提示

1. **每次啟動前都需要啟動虛擬環境**
   - 使用 `source venv/bin/activate`
   - 啟動後，終端機提示符會顯示 `(venv)`

2. **關閉虛擬環境**
   - 使用 `deactivate` 命令

3. **如果虛擬環境不存在**
   - 執行 `./啟動.sh` 會自動建立

## 🔧 如果遇到問題

### 問題：虛擬環境未建立

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 問題：依賴未安裝

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 問題：streamlit 命令找不到

確認已啟動虛擬環境：
```bash
source venv/bin/activate
# 現在應該可以使用 streamlit 命令了
```

## 🎉 開始使用

執行以下命令即可：

```bash
./啟動.sh
```

或

```bash
source venv/bin/activate
streamlit run main.py
```

瀏覽器會自動打開 `http://localhost:8501`！

