🚀 GoldenBonus AI 啟動說明
============================

您已經在正確的目錄中了！不需要再 cd。

問題：streamlit 命令找不到
解決方案：使用 python3 -m streamlit 來執行

✅ 正確的啟動命令：

python3 -m streamlit run main.py

或者，如果已經安裝了 streamlit：

streamlit run main.py

📝 完整步驟：

1. 確認當前目錄（應該已經是 golden_bonus_project）：
   pwd

2. 安裝依賴（如果還沒安裝）：
   python3 -m pip install --user streamlit google-generativeai python-dotenv

3. 啟動應用：
   python3 -m streamlit run main.py

4. 瀏覽器會自動打開，通常是 http://localhost:8501

⚠️ 注意：
- 如果使用 python3 -m streamlit，不需要先安裝到系統 PATH
- 使用 --user 參數安裝到用戶目錄，不需要 sudo

