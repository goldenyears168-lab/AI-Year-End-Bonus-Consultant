# 📦 安裝與啟動指南

## ⚠️ 當前狀況

您的系統 Python 環境有特殊限制（PEP 668），需要手動安裝依賴。

## 🔧 安裝步驟

### 方法一：使用 --user 安裝（推薦）

在終端機執行以下命令（**您已經在 golden_bonus_project 目錄中，不需要再 cd**）：

```bash
# 安裝所有依賴
python3 -m pip install --user streamlit google-generativeai python-dotenv
```

### 方法二：使用虛擬環境（更乾淨）

```bash
# 建立虛擬環境
python3 -m venv venv

# 啟動虛擬環境
source venv/bin/activate

# 安裝依賴
pip install -r requirements.txt

# 啟動應用
streamlit run main.py
```

### 方法三：使用 pipx（如果已安裝）

```bash
pipx install streamlit
streamlit run main.py
```

## 🚀 啟動應用

安裝完成後，執行：

```bash
# 方法 1：使用 python3 -m streamlit（推薦）
python3 -m streamlit run main.py

# 方法 2：如果 streamlit 已在 PATH 中
streamlit run main.py

# 方法 3：使用啟動腳本
./立即啟動.sh
```

## ✅ 驗證安裝

安裝後，可以執行以下命令驗證：

```bash
# 檢查 Streamlit
python3 -c "import streamlit; print('✅ Streamlit 已安裝')"

# 檢查 Gemini API
python3 -c "import google.generativeai; print('✅ google-generativeai 已安裝')"

# 檢查 dotenv
python3 -c "from dotenv import load_dotenv; print('✅ python-dotenv 已安裝')"
```

## 🎯 快速開始（推薦流程）

```bash
# 1. 確認您在正確的目錄
pwd
# 應該顯示：.../golden_bonus_project

# 2. 安裝依賴（只需要執行一次）
python3 -m pip install --user streamlit google-generativeai python-dotenv

# 3. 啟動應用
python3 -m streamlit run main.py
```

## ⚠️ 常見問題

### 問題：pip 安裝失敗，提示 PEP 668

**解決方案**：
- 使用 `--user` 參數：`python3 -m pip install --user <套件名>`
- 或使用虛擬環境（方法二）

### 問題：streamlit 命令找不到

**解決方案**：
- 使用 `python3 -m streamlit run main.py` 代替 `streamlit run main.py`
- 或將 Python 用戶目錄加入 PATH

### 問題：ModuleNotFoundError

**解決方案**：
- 確認已執行安裝命令
- 確認使用相同的 Python 解釋器（python3）

## 📝 下一步

安裝完成後，瀏覽器會自動打開 `http://localhost:8501`，您就可以開始使用 GoldenBonus AI 了！

